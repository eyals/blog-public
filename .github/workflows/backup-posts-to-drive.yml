name: Backup Posts to Google Drive

on:
  push:
    branches: [ main ]
    paths:
      - 'posts/**'

jobs:
  backup:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Create posts backup archive
      run: |
        # Create a timestamped zip file of the posts directory
        TIMESTAMP=$(date +%Y%m%d-%H%M%S)
        zip -r "posts-backup-${TIMESTAMP}.zip" posts/
        echo "BACKUP_FILE=posts-backup-${TIMESTAMP}.zip" >> $GITHUB_ENV
        
    - name: Upload to Google Drive
      uses: mathisve/gdrive-upload-action@v1
      with:
        filename: ${{ env.BACKUP_FILE }}
        credentials: ${{ secrets.GDRIVE_CREDENTIALS }}
        parent_folder_id: ${{ secrets.GDRIVE_FOLDER_ID }}
        
  # TODO: Add email notification setup
  # notify-failure:
  #   runs-on: ubuntu-latest
  #   needs: backup
  #   if: failure()
  #   
  #   steps:
  #   - name: Send failure notification
  #     uses: dawidd6/action-send-mail@v3
  #     with:
  #       server_address: smtp.gmail.com
  #       server_port: 587
  #       username: ${{ secrets.EMAIL_USERNAME }}
  #       password: ${{ secrets.EMAIL_PASSWORD }}
  #       subject: "Blog Backup Failed - ${{ github.repository }}"
  #       body: |
  #         The automated backup of blog posts to Google Drive has failed.
  #         
  #         Repository: ${{ github.repository }}
  #         Branch: ${{ github.ref }}
  #         Commit: ${{ github.sha }}
  #         Workflow: ${{ github.workflow }}
  #         
  #         Please check the GitHub Actions logs for more details:
  #         ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
  #       to: ${{ secrets.EMAIL_TO }}
  #       from: Blog Backup System