<!DOCTYPE html>
<html lang="en" class="text-base">
<head>
{{> html-head title=(or seo_title title) description=excerpt image=image_url type="article" publishedTime=publishedTime fullUrl=fullUrl defaultImage=defaultImage}}
    <style>
        .prose {
            color: #334155;
            max-width: none;
            line-height: 1.6;
            font-size: 1rem;
            letter-spacing: -0.03em;
        }
        .prose h1 {
            font-family: 'Space Grotesk', sans-serif;
            color: #334155;
            font-weight: 500;
            font-size: 3rem;
            line-height: 1.1;
            letter-spacing: -0.05em;
            margin-top: 2rem;
            margin-bottom: 1rem;
        }
        .prose h2 {
            font-family: 'Space Grotesk', sans-serif;
            color: #334155;
            font-weight: 500;
            font-size: clamp(1.5rem, 2.5vw, 1.7rem);
            line-height: 1.2;
            letter-spacing: -0.05em;
            margin-top: 2rem;
            margin-bottom: 1rem;
            padding-left: 2rem;
            padding-right: 2rem;
        }
        .prose h3 {
            font-family: 'Space Grotesk', sans-serif;
            color: #334155;
            font-weight: 500;
            font-size: 1.5rem;
            line-height: 1.3;
            letter-spacing: -0.05em;
            margin-top: 1.5rem;
            margin-bottom: 0.5rem;
            padding-left: 2rem;
            padding-right: 2rem;
        }
        .prose h4 {
            font-family: 'Space Grotesk', sans-serif;
            color: #334155;
            font-weight: 500;
            font-size: 1.25rem;
            line-height: 1.3;
            letter-spacing: -0.05em;
            margin-top: 1.5rem;
            margin-bottom: 0.5rem;
            padding-left: 2rem;
            padding-right: 2rem;
        }
        .prose p {
            margin-top: 1rem;
            margin-bottom: 1rem;
            padding-left: 2rem;
            padding-right: 2rem;
        }
        .prose a {
            color: #334155;
            text-decoration: underline;
            font-weight: 500;
        }
        .prose code {
            background-color: #f3f4f6;
            padding: 0.125rem 0.25rem;
            border-radius: 0.25rem;
            font-size: 0.875rem;
            font-weight: 500;
        }
        .prose pre {
            background-color: #1f2937;
            color: #f9fafb;
            padding: 1rem;
            border-radius: 0.5rem;
            overflow: auto;
            font-size: 0.875rem;
        }
        .prose blockquote {
            border-left: 4px solid #e5e7eb;
            padding-left: 1rem;
            font-style: italic;
            color: #6b7280;
        }
        .prose ul {
            list-style-type: disc;
            padding-left: 3.5rem;
            padding-right: 2rem;
        }
        .prose ol {
            list-style-type: decimal;
            padding-left: 3.5rem;
            padding-right: 2rem;
        }
        .prose li {
            margin-top: 0.5rem;
            margin-bottom: 0.5rem;
        }
        .prose img {
            border-radius: 0.5rem;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }
    </style>
</head>
<body class="font-jetbrains-mono text-slate-700 bg-white">
    <div class="min-h-screen">
        <article class="max-w-[45rem] mx-auto px-4 py-8">
            <header class="text-center mb-12">
                <!-- Logo at top - links to root -->
                <div class="mb-[100px]">
                    <a href="/">
                        <img 
                            src="/overtink-logo.svg" 
                            alt="Overtink Logo" 
                            class="w-[180px] mx-auto"
                        />
                    </a>
                </div>
                
                <!-- Centered title -->
                <h1 class="font-space-grotesk font-medium text-post-title text-slate-700 mb-4">
                    {{title}}
                </h1>
                
                <!-- Author and date -->
                <div class="font-jetbrains-mono text-body text-slate-700 mb-8">
                    By <a href="https://ey.al" class="underline hover:no-underline">Eyal Shahar</a>
                    <span class="ml-5">{{formatDateMonthYear publish_date}}</span>
                </div>
                
                <!-- Main image below date -->
                {{#if image_url}}
                <div class="mb-8 -mx-4">
                    <img 
                        src="{{image_url}}" 
                        alt="{{title}}" 
                        class="w-full rounded-lg shadow-lg"
                        style="aspect-ratio: 5/3; object-fit: cover;"
                    />
                </div>
                {{/if}}
            </header>
            
            <div class="mb-8">
                <div class="prose max-w-none">
                    {{{processMarkdown content}}}
                </div>
            </div>
        </article>
    </div>
</body>
</html>