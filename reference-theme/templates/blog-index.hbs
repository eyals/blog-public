<!DOCTYPE html>
<html lang="en" class="text-base">
<head>
{{> html-head}}
</head>
<body class="font-jetbrains-mono text-slate-700 bg-white">
    <div class="min-h-screen">
        <div class="max-w-6xl mx-auto px-[3vw] py-8">
            <!-- Header with logo and tagline -->
            <header class="text-center pt-[75px] min-[500px]:pt-[100px] pb-[75px] min-[500px]:pb-[100px]">
                <div class="mb-[25px]">
                    <img 
                        src="/overtink-logo.svg" 
                        alt="Overtink Logo" 
                        class="w-[300px] min-[500px]:w-[500px] mx-auto"
                    />
                </div>
                <h1 class="font-space-grotesk font-medium text-secondary-title text-slate-700">
                    The Restless 🧠<br>
                    and Impatient 🙌<br>
                    of <a href="https://ey.al" class="underline hover:no-underline">Eyal Shahar</a>
                </h1>
            </header>

            <main>
                {{#if posts}}
                <div class="space-y-[60px]">
                    {{#each posts}}
                    <article class="cursor-pointer hover:opacity-80 transition-opacity duration-200" onclick="window.location.href='/{{slug}}'">
                        <div class="flex flex-col min-[500px]:flex-row {{#ifEqual (mod @index 2) 0}}min-[500px]:flex-row-reverse{{/ifEqual}} items-start gap-[10vw]">
                            {{#if image_url}}
                            <div class="min-[500px]:w-1/2 w-full">
                                <img 
                                    src="{{image_url}}" 
                                    alt="{{title}}" 
                                    class="w-full h-64 min-[500px]:h-80 object-cover rounded-lg"
                                />
                            </div>
                            {{/if}}
                            <div class="min-[500px]:w-1/2 w-full flex flex-col justify-center text-center min-[500px]:min-h-80">
                                <h2 class="font-space-grotesk font-medium text-slate-700 mb-4 text-[clamp(2rem,5vw,2.5rem)] leading-[1.1] tracking-[-0.05em]">
                                    {{title}}
                                </h2>
                                <time class="font-jetbrains-mono text-body text-slate-700">
                                    {{formatDate publish_date}}
                                </time>
                            </div>
                        </div>
                    </article>
                    {{/each}}
                </div>
                {{else}}
                <div class="text-center py-16">
                    <p class="font-jetbrains-mono text-body text-slate-700">No published posts yet.</p>
                </div>
                {{/if}}
            </main>
        </div>
    </div>
</body>
</html>